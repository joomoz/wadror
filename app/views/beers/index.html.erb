<h1>Listing Beers</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Style</th>
      <th>Brewery</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @beers.each do |beer| %>
      <tr>
        <td><%= link_to beer.name, beer %></td>
        <td><%= beer.style %></td>
        <td><%= link_to beer.brewery.name, beer.brewery %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% if current_user%>
  <div class="btn btn-info btn-sm">
    <%= link_to 'Create a new beer', new_beer_path %>
  </div>
<% end %>
<div class="btn btn-info btn-sm">
  <%= link_to 'List of Breweries', breweries_path %>
</div>
